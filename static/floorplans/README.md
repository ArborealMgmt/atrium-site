# Floor plans (legacy path)

Floor plan artwork for the Apartments page now lives as PNGs in **`static/images/floor-plans/`** (e.g. `unit-a3.png`, `unit-b2.png`, …).

The previous PDFs in this folder have been removed in favor of those rendered images.
